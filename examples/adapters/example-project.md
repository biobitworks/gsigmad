# Adapter: Example Project

**Path:** /absolute/path/to/example-project

has_canon: false

**EXP namespace prefix:** `example:`

This is a public placeholder. Replace it in a private project-specific adapter
directory when connecting `gsigmad` to local repositories.
