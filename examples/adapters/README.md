# Adapter Examples

This public repo intentionally excludes private runtime adapters.

Use these examples as templates for local projects. Keep project-specific
secrets, private paths, gated datasets, and live writeback configuration out of
public repositories.
