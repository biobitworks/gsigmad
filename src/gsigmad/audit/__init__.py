"""gsigmad.audit — deterministic audit harnesses.

Stdlib-only harnesses that map governance artifacts to PASS / PASS_WITH_BLOCKS
/ REJECT verdicts without any LLM or network call. These harnesses back the
gsigmad-audit-* skill SOPs so experiments can be re-run deterministically.
"""
